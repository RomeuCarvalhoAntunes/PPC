#include <bits/stdc++.h>

using namespace std;

int main(){

	int a,b,d,e;
	double c,f;

	cin >> a >> b;
	cin >> c;
	cin >> d >> e; 
	cin >> f;

	cout << fixed << setprecision(2);

	cout << "VALOR A PAGAR: R$ " << (b*c)+(e*f) << endl;

	return 0;

}
