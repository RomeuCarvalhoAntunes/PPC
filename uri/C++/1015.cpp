#include <bits/stdc++.h>

using namespace std;

int main(){
	double a,b,c,d;

	cin >> a >> b >>c >> d;

	cout << fixed << setprecision(4);

	cout << sqrt(pow(c-a,2) + pow(d-b,2)) << endl; 

	return 0;
}
